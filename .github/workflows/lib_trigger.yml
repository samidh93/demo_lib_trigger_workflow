name: Trigger lib Api
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        #checkout repo code 
      - name: Check Out Repo 
        uses: actions/checkout@v2
        with:
          submodules: 'true'
          token: ${{ secrets.TEST }}
      - name: Build Web App
        run: |  
          curl -i -H "Authorization: token ${{ secrets.TEST }}" \
          https://api.github.com/users/theionSami/repos     
      - name: Deploy Web App
        run: |
          echo "Deploy Web App"
          sleep 10s
